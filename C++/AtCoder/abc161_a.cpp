#include<iostream>
using namespace std;
int main()
{
    short X,Y,Z;
    cin>>X>>Y>>Z;
    cout<<Z<<" "<<X<<" "<<Y<<'\n';
    return 0;
    // Or we can use a temp variable to carry the value of X for e.g when swaping X and Y then the new value of X when swaping X and Z
    // Or we can use swap() function
}